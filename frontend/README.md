# Port-Intelli Frontend

This is the React, TypeScript, Vite, and Tailwind CSS frontend for Port-Intelli.

Run it locally from this directory:

```bash
npm install
npm run dev
```

The app serves on port `8080` and calls the backend API at `http://127.0.0.1:8000` unless `VITE_API_URL` is set.
